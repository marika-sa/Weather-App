# Weather-App
A small web application using C# that allows a user to  receive weather, timezone and astronomy information related to 3 cities. The application has a simple UI to allow the user to select a single city from a list of 3 and to then see the weather associated with that city. 
